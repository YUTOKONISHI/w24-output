# 要件定義書
## 1. アプリの目的
- 保存のきく食品や日用品の在庫切れを防ぎ、買い忘れによる生活の不便を解消するため

## 2. ユーザー要件
- 一般ユーザー
  - 一人暮らしの社会人または主婦
  - 食品や日用品のストックを管理したい
  - 定期購入の目安期間を設定したい

- 管理者
  - 食品・日用品のマスタ管理を行う 

## 3. 機能要件
### トップページ
- サービス概要を記載
- ログイン画面への導線

### 新規登録
- アカウント作成（ユーザ名 / メールアドレス / パスワード ）

### ログイン
- メールアドレス、パスワードでログイン
- ログイン成功時、ダッシュボードに遷移

### 設定
- 個人情報の変更
  - ユーザ名、パスワード、世帯人数の変更

### パスワード忘却
- パスワードのリセット

### パスワード再設定
- メールに記載された仮パスワードと新規パスワードを入力

### ダッシュボード
- 次回購入日とカレンダー（月単位）を表示
  - カレンダーは全件の予定日に枠を出す。下の1週間の絞り込みは掛けない
- 購入予定品を表示
   - ストック数を1のものを優先表示
   - **1週間以内に予定日が来るものだけ表示する**（2026-08-29 決定）。件数の表示も同じ範囲で数える
   - **購入ボタンで購入済みを記録する**（2026-08-29 追加）。次回購入予定日が「今日 + 消費日数 × ストック数」に進む。ストック数は変えない

### ストック管理
- 商品ごとに消費日数とストック数を登録
  - 消費日数は世帯人数を元に初期値を設定するが、手動で修正可能
  - ストック数の設定（手動入力）
  - 次回購入予定日は「最終更新日 + （消費日数 × ストック数）」で自動算出する（手動での上書きも可能） 
  - **同じ商品を重ねて登録できない**（2026-08-28 追加）。編集では商品を変更できない

### Push通知画面
- 通知設定のON/OFF
  - ONかOFFかの正本は端末が持つ。サーバに設定列は無い（`doc/plans/20260817-push-notification.md`）
- 通知内容(当日の買い忘れ防止)の履歴を表示
  - **未読のものだけ表示する**（2026-08-29 決定）。チェックで既読にすると画面から消え、トーストから戻せる
- 当日朝に通知（例：9時） 
  - **購入予定日を過ぎたものにも毎朝通知する**（2026-08-29 決定）。通知が止まるのはダッシュボードの購入ボタンだけで、履歴を既読にしても止まらない

### 管理者向けログイン画面
- ユーザ名、パスワードでログイン
- ログイン成功時、管理者用マスタ画面に遷移

### 管理者用マスタ画面
- 食品・日用品のCRUD
- **カテゴリのCRUD**（2026-08-16 追加、`doc/plans/20260815-requirement-gap-fixes.md`）
- パスワード変更（モーダル）
- 使用中の商品とカテゴリは削除できない

## 4. 非機能要件
- サポートブラウザ
    - Chrome

- レスポンシブ対応
    - 管理画面を除く

- **PWA対応**（2026-08-01 追加、`doc/plans/20260729-pwa-general-user-only.md`）
    - 一般ユーザー画面（`/app/*`）のみ。管理画面はService Workerのスコープ外
    - オフラインではオフライン用の画面を出す。データの閲覧はしない

- ログ設計
   - APIリクエストおよびエラーログの記録 
   - 実装済み。現行の姿は `doc/design/non-functional.md`、決めた経緯は `doc/plans/20260823-request-error-logging.md` にある

- 認証 
  - ~~Laravel Sanctum認証~~ → **Fortify のセッション認証を正とする**（2026-08-15 決定）

- セキュリティ
    - XSS / CSRF対策

- API設計
  - ~~REST API~~ → **Inertia を正とする**（2026-08-15 決定）

### 認証とAPI設計を変えた理由
現行は Inertia とセッション認証（Fortify の `web` ガードと `admin` ガード）で動いている。
API化は既存画面すべてに波及するため実施しない。
経緯は `doc/plans/20260815-requirement-gap-fixes.md` にある。

## 5. 主要な画面構成・一部
- 一般ユーザー
  - ログイン画面
  - 新規登録画面
  - パスワード忘却画面
  - ダッシュボード画面
  - Push通知画面 
  - ストック管理画面
- 管理者
  - 管理者用ログイン画面
  - 管理者用マスタ管理画面 

## 6. 技術スタック
- フロントエンド言語：HTML/CSS/JavaScript
    - 使用するフレームワーク：React
- サーバーサイド言語：PHP
    - 使用するフレームワーク：Laravel
- DB：PostgreSQL

# 基本設計書
本書が「何を作るか」を書くのに対し、「どう作られているか」は `doc/design/` にある。
索引は `doc/design/README.md`。
コードが正本で、設計書はその写しである。

# ペルソナ設定
* 項目\t内容
* 名前\t鈴木ひまり
* 性別\t女性
* 年齢\t30歳
* 家族構成\t夫（32歳）
* 住まい\t埼玉
* 趣味\t料理・お菓子作り
* 職業\tデザイナー
* 収入\t300万
* よく使うSNS\tInstagram

## 主な情報源
* 雑誌
* 休日のネットサーフィン
# どんな生活をしている人？
* 夫との2人暮らし。新婚でようやく生活に慣れ始めてきた頃で、平日の家事分担がうまく回るようになってきた。結婚前はお互い好きなことに時間とお金を使っていたが、結婚式の軍資金を貯めるために生活の見直しをしている。

## 意思決定に重要な要素は？
* わかりやすさ、使い心地、親しみやすさ

## 商品を購入しようと思った理由・きっかけは？
* ストックが切れそうな日用品を夫婦別々のタイミングで購入してしまうことがあったり、まだストックのある日用品や食品（日持ちするもの）を週末のまとめ買いの際にうっかり購入してしまうことがある。効率的なストック管理がしたく、購入を検討した。

# デザインモック
- doc/specifications/ストック管理システム.png

# DB設計
- https://drawsql.app/teams/konishi/diagrams/portfolio/docs
- エクスポート: doc/specifications/drawSQL-pgsql-export-2026-08-28.sql
- 図の正本はdrawSQL側にある。設計を変えたらエクスポートし直す

# 開発ルール

## 前提条件
- 不明瞭な点がある場合は必ず人間に確認を取ること

## 実行環境
- Laravel Sail で開発している。アプリケーションはDockerコンテナ上で動く
- ホストにnodeもphpも無い。npmもartisanもcomposerもコンテナ経由で叩く
- コマンドは `./vendor/bin/sail ...` に揃える（例: `./vendor/bin/sail composer ci:check`）
- ただしAIエージェントが検証で叩くときは `docker compose exec -T laravel.test ...` を使う。`vendor/bin/sail` は `AI_AGENT` や `CLAUDECODE` をコンテナへ転送し、pintとphpstanがエージェント向けの出力に切り替わる。phpstanは出力を返さず終了コードだけを返すため、問題が無くても失敗に見える

## Laravelの書き方の指針
- Laravel Boost が配る指針は `doc/boost-guidelines.md` にある。`boost:install` の既定はAGENTS.mdとCLAUDE.mdだが、本書の内容が埋もれるので `config/boost.php` で向き先を変えている
- 生成物なので手で編集しない。`composer update` のたびに `boost:update` が書き換える
- 本書の記述と食い違う場合は本書を優先する

## 計画書
doc\plans配下の要件概要書をもとに計画を作成する
- YYYYMMDD-計画名.md
- 記載ルールは追記式として原則書き換えない
- 長さは中身に見合わせる。埋め草の節、同じ内容の繰り返し、定型文で膨らませない

## 作業の進め方
- 頼まれた範囲のものを、頼まれた粒度で出す。黙って狭めたり広げたり作り変えたりしない
- 依頼が誤っていそうなとき、より良いやり方があるときは、一文で述べてから頼まれたとおりに進める
- 検証は自分で判断して行う。「最終検証の工程を必ず入れる」といった指示は書かない
- サブエージェントへの委任は、独立して並行できる大きな調査に限る。小さな作業では時間と費用が倍になるだけ

## コメント
- 開発者は1人。計画書を読めば分かることをコードコメントに書かない
- 設計に合わせた理由、採用しなかった案、経緯は計画書の役目
- 書いてよいのは、コードを読むだけでは分からない事実に限る。値の形式、他の関数との責任分担、言語仕様の制約など
- 同じ文言を複数のファイルに並べない
- メソッド名を言い換えただけのdocblockを付けない

## コードレビュー
- 「重大度の高いものだけ報告」「保守的に」とは指示しない。指示どおりに報告が減る
- 全部出させてから、別の工程で絞り込む

# 応答の長さと進め方の伝え方
- 焦点を絞って簡潔に書く。前置きと注意書きは短くし、本題に紙面を使う
- 説明を求められたときは概要を答える。詳細は求められてから書く
- 最初のツール呼び出しの前に、これから何をするかを一文で述べる
- 作業中の報告は、重要なことが分かったときと方針を変えるときだけにする
- 終わったら結果から書く。最初の一文で「何が起きたか」「何が分かったか」に答え、詳細はその後に置く

# 日本語応答のスタイル
チャットの返答、コミットメッセージ、計画書、コメント、ドキュメントのいずれにも適用する。

- ダッシュ（──、——）を地の文で使わない。同格・補足の挿入は括弧（）に、言い換えは読点でつなぐか二文に分ける。
- 英語概念の直訳（発射、着弾、不発など）や、意味の近い漢語の当て込みを避ける。その分野で慣用される平易な語を選ぶ。
- その場で発明した圧縮ラベル（「ペイン死」「不遷移」など）を、定義なしで使わない。まず普通の言い方で述べ、名前が要るなら一言で定義してから使う。
- 空虚な強調・前置き（「重要なのは」「非常に」「包括的」など）を書かない。主張をそのまま書く。
- 一度導入した術語は、言い換えずその語で通す。
- 比喩は説明の道具として使ってよい。ただし正本のドキュメントに持ち込むときは選別する。

日本語の技術文書（記事、章、解説文、設計文書）を書く・推敲するときは、さらに `.claude/skills/japanese-tech-writing/SKILL.md` の規範に従う。